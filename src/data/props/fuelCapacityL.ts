import { Prop } from "../../utils/prop";

/**
 * Fuel tank capacity in liters (for diesel/fuel-powered vehicles).
 * @example 6000 for a typical diesel locomotive
 */
export const fuelCapacityLProp = new Prop<number>("fuelCapacityL");
