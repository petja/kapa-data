import { Prop } from "../../utils/prop";

/**
 * Paint scheme or livery name.
 * @example "DB red", "InterCity", "BR blue", "Original works grey"
 */
export const liveryProp = new Prop<string>("livery");
