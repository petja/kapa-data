import { Prop } from "../../utils/prop";

/**
 * Maximum acceleration in meters per second squared.
 * @example 1.1 for a typical commuter EMU
 */
export const accelerationMps2Prop = new Prop<number>("accelerationMps2");
