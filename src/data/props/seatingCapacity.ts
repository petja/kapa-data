import { Prop } from "../../utils/prop";

/**
 * Number of seats in the vehicle.
 * @example 68 for a typical intercity coach
 */
export const seatingCapacityProp = new Prop<number>("seatingCapacity");
