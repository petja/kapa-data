import { Prop } from "../../utils/prop";

/**
 * Cargo load capacity in metric tonnes.
 * @example 60 for a typical freight wagon
 */
export const cargoCapacityTProp = new Prop<number>("cargoCapacityT");
