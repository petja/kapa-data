import { Prop } from "../../utils/prop";

/**
 * Distance between wheel centers in millimeters.
 * @example 2500 for a typical bogie wheelbase
 */
export const wheelbaseMmProp = new Prop<number>("wheelbaseMm");
