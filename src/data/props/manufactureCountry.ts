import { Prop } from "../../utils/prop";

/**
 * Country where the vehicle was manufactured in (ISO 3166-1 alpha-2 code)
 */
export const manufactureCountryProp = new Prop<string>("manufactureCountry");
